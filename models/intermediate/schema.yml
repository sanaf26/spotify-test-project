version: 2

models:
  - name: int_track_artists
    columns:
      - name: track_id
        tests:
          - not_null
          - relationships:
              to: ref('stg_spotify_tracks')
              field: track_id

      - name: artist_name
        tests:
          - not_null